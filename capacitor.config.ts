import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourdomain.photogallery',
  appName: 'photo-gallery',
  webDir: 'dist'
};

export default config;
